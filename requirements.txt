# HEMOCAST / NVCB Streamlit App — requirements.txt
# For: nvcb_streamlit.py (the deployed app — NOT the training notebook)
# The app only LOADS the pre-trained GBM model; it never re-trains.
#
# Pin strategy:
#   - scikit-learn: MUST match the version used to save nvcb_gbm_model.pkl
#   - joblib: pin to the version bundled with that scikit-learn release
#   - Everything else: floor-pinned (>=) so Streamlit Cloud can resolve freely
#
# ⚠️  IMPORTANT: If you trained the model with scikit-learn 1.4.2 (previous
#     deployment), keep scikit-learn==1.4.2 below. If you retrain with a
#     newer version, update this pin to match.

streamlit>=1.32.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn==1.4.2
joblib>=1.3.0
openpyxl>=3.1.0
